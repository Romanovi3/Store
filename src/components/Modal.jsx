import React from 'react';

const Modal = ({description, image, price, category, title}) => {
    return (
        <div className='absoule vh-60vh'>
            <h2>{title}</h2>
        </div>
    );
};

export default Modal;